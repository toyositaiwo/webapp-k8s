apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: webapp-secret
  namespace: production
  annotations:
    sealedsecrets.bitnami.com/namespace-wide: "true"
spec:
  encryptedData:
    DB_PASSWORD: AgBy3i4OCNI4EXAMPLE...REPLACE_WITH_REAL_SEALED_VALUE==
    API_KEY: AgBy3i4OCNI4EXAMPLE...REPLACE_WITH_REAL_SEALED_VALUE==
  template:
    metadata:
      name: webapp-secret
      namespace: production
